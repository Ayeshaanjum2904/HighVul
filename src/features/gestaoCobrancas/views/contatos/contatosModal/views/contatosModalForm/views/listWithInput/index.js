import { connect } from 'react-redux';
import ListWithInput from './listWithInput';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({
});
export default connect(mapStateToProps, mapDispatchToProps)(ListWithInput);
