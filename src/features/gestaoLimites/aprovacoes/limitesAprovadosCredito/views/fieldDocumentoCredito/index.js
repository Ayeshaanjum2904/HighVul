import { connect } from 'react-redux';
import FieldDocumentoCredito from './fieldDocumentoCredito';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({
});
export default connect(mapStateToProps, mapDispatchToProps)(FieldDocumentoCredito);
