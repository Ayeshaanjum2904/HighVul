import { connect } from 'react-redux';
import ErroPage from './erroPage';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(ErroPage);
